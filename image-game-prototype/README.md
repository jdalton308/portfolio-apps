
This is a starting point for most of my web-based projects.  It uses Gulp for a task-manager and Bower for a dependency-manager.

After downloading,

1. Run `npm i`, then `bower install`
2. Run `npm run build`
3. Start a static site server from the root directory, such as `static-server`